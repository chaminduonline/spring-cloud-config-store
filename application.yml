message: Hi Spring Cloud Config

server:
  port: 8190
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chaminduonline/spring-cloud-config
          search-paths:
           - '*service'
